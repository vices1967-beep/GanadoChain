// Auto-generated slice for reportsSlice.ts
import { createSlice } from '@reduxjs/toolkit';

const src/store/slices/reportsSlice = createSlice({
  name: 'src/store/slices/reportsSlice',
  initialState: src/store/slices/reportsSlice.ts,
  reducers: src/store/slices/reportsSlice.ts,
});

export const src/store/slices/reportsSlice.ts = src/store/slices/reportsSlice.actions;
export default src/store/slices/reportsSlice.reducer;
